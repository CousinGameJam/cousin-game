class UI extends Phaser.Scene {

    // this overlays the gameplay scene but does not scroll, zoom or move
    
    constructor() {
        super({ key: 'UI', active: true });
    }

    preload() {
    }

    create() {
    }

    update() {
    }

}

export default UI